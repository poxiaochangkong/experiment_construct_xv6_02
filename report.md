实验目标
    通过参考 xv6 的输出系统，理解格式化字符串的处理原理，独立实现功能完整的内核 printf 函数，并利用 ANSI 转义序列实现清屏和清行功能。
系统设计部分
    架构设计部分
        为了实现格式化输出，本次实验构建了一个分层的输出系统架构，将复杂的格式处理与底层的硬件操作解耦。
        整个输出流程分为三个层次，调用关系为 printf() -> consputc() -> uart_putc()：
        格式化层 (printf.c): 这是最高层，负责对外提供 printf 接口。它的核心任务是解析格式化字符串（如 %d, %s, %x），并利用 C 语言的 stdarg.h 库处理可变参数。根据不同的格式说明符，它会调用相应的函数（如 printint, printptr）将整数、指针、字符串等不同类型的数据转换为字符流，并逐一传递给下一层。
        控制台层 (console.c): 这一层是硬件和高级功能之间的抽象。它提供了 consputc 函数，作为所有字符输出的统一入口。这一层未来可以扩展，比如将字符同时输出到串口和显存。此外，它还处理一些特殊的控制字符，例如将退格符（BACKSPACE）解释为“后退-空格-再后退”的擦除操作。
        硬件驱动层 (uart.c): 这是最底层，直接与硬件交互。uart_putc 函数负责将单个字符通过 MMIO（内存映射 I/O）写入串口的 THR（发送保持寄存器）。
    关键数据结构
        va_list: 这是 printf 实现的核心，用于在运行时访问数量不定的函数参数。通过 va_start 初始化，va_arg 依次获取参数，va_end 清理，实现了类型安全的可变参数处理。
        digits[]: 在 printf.c 中定义的静态字符数组 static char digits[] = "0123456789abcdef";，它作为一个查找表，用于在 printint 函数中快速地将数字的余数（0-15）映射到对应的字符表示。
    与xv6对比分析
        分层架构: 两者都采用了 printf -> consputc -> uartputc 的分层设计，体现了良好的软件工程思想。
        锁机制: xv6 的 printf 和 consputc 函数包含了锁机制，以确保在多核或中断环境下输出不会被打乱。本实验的实现是单线程的，尚未引入并发控制，因此没有加锁。

实验过程部分
    实验步骤
        实现数字转换: 在 printf.c 中，首先实现 printint 函数。该函数通过循环取余和除法操作，将一个 long long 类型的整数转换为指定进制（10或16）的字符串，并存入一个临时缓冲区 buf，最后再逆序输出。这种“先转换后逆序输出”的方式巧妙地避免了递归。
        实现 printf 主体: 编写 printf 函数，使用 va_list 相关的宏来处理可变参数。主逻辑是一个 for 循环，逐字符遍历格式字符串。遇到 % 时，检查后续字符来判断参数类型，并调用 va_arg 获取参数，然后传递给 printint, printptr 或直接输出。
        建立控制台抽象层: 创建 console.c 文件，并实现 consputc 函数。这为 printf 提供了一个统一的字符输出接口。
        实现清屏功能: 在 console.c 中，实现 clear_screen 和 clear_line 函数，通过调用 printf 输出特定的 ANSI 转义序列，结合现代化的控制台即可实现清屏，清除行等功能。
        更新构建系统: 修改 Makefile，在 OBJS 变量中加入 kernel/printf.o 和 kernel/console.o，确保新文件能被正确编译和链接。
    问题与解决方案
        处理 INT_MIN：INT_MIN 的绝对值比 INT_MAX 大1，直接取反 -INT_MIN 会导致有符号整数溢出。
        在 printint 函数中，目标变量 x 被定义为 unsigned long long 类型。当 xx 为负数时，x = -xx 这个操作会将负数转换为其绝对值的正数。对于 INT_MIN，这个转换在 unsigned long long 的范围内是安全且定义明确的，从而避免了溢出问题。
测试验证部分
    执行make run 后观察命令行的输出后即可（需要关掉该bash否则会因为死循环大量占用系统资源）。
    